export const enum CardType {
  Monster = "monster",
  Spell = "spell",
  Trap = "trap",
}

export const enum Phase {
  Draw = 0,
  Main1,
  Battle,
  Main2,
  End,
}
export const PHASE_ENUM_LENGTH = 5;
